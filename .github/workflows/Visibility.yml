name: Change Visibility

on:
  workflow_dispatch:
  
    
        
 
jobs:
  build:
    runs-on: ubuntu-slim
    steps:

    - name: GitHub Visibility Action
      uses: JustinBird/github-visibility-action@v0.0.2
