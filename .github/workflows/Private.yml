name: Private

on:
  workflow_dispatch:

jobs:
  change-visibility:
    runs-on: ubuntu-slim
    steps:
      - name: Change Visibility
        uses: JustinBird/github-visibility-action@v1
        with:
          visibility: private
          token: ${{ secrets.ADMINISTRATION_TOKEN }}
